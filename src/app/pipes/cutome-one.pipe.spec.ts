import { CutomeOnePipe } from './cutome-one.pipe';

describe('CutomeOnePipe', () => {
  it('create an instance', () => {
    const pipe = new CutomeOnePipe();
    expect(pipe).toBeTruthy();
  });
});
